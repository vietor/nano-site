# AI

## 规则

- 所有 `Link` 组件必须加 `prefetch={false}`
- 所有界面文本使用中文
- Git 提交信息使用中文，格式：`type: 描述`
- API 路由统一使用 App Router `route.ts`，不用 Pages Router
- 生成的代码必须极简且符合生产级标准
- 生成的代码不得包含任何解释性文本或注释
- 不生成任何形式的测试代码或示例代码
- 不执行项目运行、构建或检查命令
